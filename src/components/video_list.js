import React from 'react';
// import VideoListItem from './components/video_list_item';

const VideoList = (props) => {
	return (
		//SOMETHING
		<ul className="col-mid-4 list-group" >
			<p>{props.videos.length}</p>
		</ul>
		
	);
}

export default VideoList;